// 9.1 Unrecoverable Errors with panic!

pub fn _run() {
    // cause a panic
    // panic!("crash and burn");

    // Using a panic! Backtrace
    // let v = vec![1, 2, 3];
    // v[99];
    // this panics, we can see the backtrace (think stack trace) by using the
    // RUST_BACKTRACE env variable when using cargo run
}