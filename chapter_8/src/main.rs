// Chapter 8: Common Collections
// Collections are data structures that may contain multiple types
// unlike the built in array and tuple types, the data in these collections
// is stored on the heap, which means the amount of data to be stored does not
// need to be known at compile time

mod vectors;
mod strings;

fn main() {
    // vectors::run();
    strings::run();
}
